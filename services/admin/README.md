# Admin service

- `cmd`: independently buildable admin HTTP server.
- `api`: admin transport and route assembly.
- `application`: admin use cases.
- `control`: gRPC control-plane client/server adapters.

Build this service with `services/admin/Dockerfile`.
