apiVersion: 1

providers:
  - name: LAN IM
    orgId: 1
    folder: LAN IM
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
